// Recursively build the previous rows, until we reach the very beginning of the triangle
// row represents how many rows to go back until we reach the start, the base case
const pascal = function(n, row = []) {
    if (n === 1) return [1];

};
  
// Do not edit below this line
module.exports = pascal;
